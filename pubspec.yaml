name: solucionador_de_sistemas_de_ecuaciones_no_lineales
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_math_fork: ^0.7.4
  fraction: ^5.0.5
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/M.png"
  remove_alpha_ios: true

flutter:
  uses-material-design: true
